{
  "name": "@tobydux/strongly-typed-events-mono-root",
  "private": true,
  "dependencies": {
    "@tobydux/strongly-typed-events": "*"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "build": "lerna bootstrap & lerna run build",
    "test": "lerna run test",
    "make": "lerna run build && lerna publish --yes --cd-version patch",
    "make-feature": "lerna run build && lerna publish --yes --cd-version minor"
  }
}
